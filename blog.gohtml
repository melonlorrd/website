<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/png" href="favicon.png">
  <title>{{ .Title }} - Melonlorrd's Blog</title>

  <style>
    /* =========================================================
       Layout tokens
       ========================================================= */
    :root {
      --max-width: 720px;
      --side-padding: 1.25rem;
      --radius: 6px;
      --font-body: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }

    /* =========================================================
      Catppuccin Latte — calm & airy
      ========================================================= */
    @media (prefers-color-scheme: light) {
      :root {
        /* Base */
        --bg: #eff1f5;          /* base */
        --bg-alt: #e6e9ef;      /* mantle */
        --surface: #dce0e8;     /* surface0 */

        /* Text */
        --fg: #4c4f69;          /* text */
        --fg-muted: #6c6f85;    /* subtext0 */
        --heading: #7287fd;     /* lavender */

        /* Accents */
        --link: #1e66f5;        /* blue */
        --link-hover: #209fb5; /* sapphire */

        /* UI */
        --border: #ccd0da;      /* surface2 */
        --code-bg: #e6e9ef;     /* mantle */
        --inline-code-bg: #dce0e8; /* surface0 */
      }
    }

    /* =========================================================
      Catppuccin Mocha — soft & cozy
      ========================================================= */
    @media (prefers-color-scheme: dark) {
      :root {
        /* Base */
        --bg: #1e1e2e;          /* base */
        --bg-alt: #181825;     /* mantle */
        --surface: #313244;    /* surface0 */

        /* Text */
        --fg: #cdd6f4;         /* text */
        --fg-muted: #a6adc8;   /* subtext0 */
        --heading: #b4befe;    /* lavender */

        /* Accents */
        --link: #89b4fa;       /* blue */
        --link-hover: #74c7ec; /* sapphire */

        /* UI */
        --border: #45475a;     /* surface1 */
        --code-bg: #181825;    /* mantle */
        --inline-code-bg: #313244; /* surface0 */
      }
    }

    /* =========================================================
       Base styles
       ========================================================= */
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background: var(--bg);
      color: var(--fg);
      font-family: var(--font-body);
      line-height: 1.7;
      scrollbar-color: var(--fg) var(--bg);
    }

    main {
      max-width: var(--max-width);
      margin: 3rem auto;
      padding: 0 var(--side-padding);
    }

    a {
      color: var(--link);
      text-decoration: none;
      border-bottom: 1px solid transparent;
      transition: color 0.15s ease, border-color 0.15s ease;
    }

    a:hover {
      color: var(--link-hover);
      border-color: var(--link-hover);
    }

    header {
      margin-bottom: 2rem;
    }

    h1 {
      color: var(--heading);
      font-size: clamp(1.6rem, 3vw, 2.2rem);
      line-height: 1.3;
      margin: 0.5rem 0;
    }

    time {
      font-size: 0.9rem;
      color: var(--fg-muted);
    }

    /* =========================================================
       Article content
       ========================================================= */
    article {
      margin-top: 2rem;
    }

    article p {
      margin: 1rem 0;
    }

    article h2,
    article h3,
    article h4 {
      color: var(--heading);
      margin-top: 2.5rem;
      margin-bottom: 0.75rem;
      line-height: 1.4;
    }

    article img {
      max-width: 100%;
      height: auto;
      display: block;
      margin: 1rem 0;
      border-radius: var(--radius);
    }

    article ul,
    article ol {
      padding-left: 1.5rem;
      margin: 1rem 0;
    }

    article li {
      margin: 0.4rem 0;
    }

    article blockquote {
      margin: 1.5rem 0;
      padding-left: 1rem;
      border-left: 3px solid var(--border);
      color: var(--fg-muted);
    }

    /* =========================================================
       Code
       ========================================================= */
    pre {
      background: var(--code-bg);
      padding: 1rem;
      border-radius: var(--radius);
      overflow-x: auto;
      margin: 1.5rem 0;
      font-size: 0.9rem;
    }

    code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    }

    p code,
    li code {
      background: var(--inline-code-bg);
      padding: 0.15rem 0.35rem;
      border-radius: 4px;
      font-size: 0.9em;
    }

    /* =========================================================
       Back link
       ========================================================= */
    .back {
      display: inline-block;
      margin-bottom: 1.5rem;
      font-size: 0.95rem;
    }

    /* =========================================================
       Responsive tweaks
       ========================================================= */
    @media (max-width: 600px) {
      main {
        margin: 2rem auto;
      }

      pre {
        font-size: 0.85rem;
      }
    }
  </style>
</head>

<body>
  <main>
    <a class="back" href="index.html">Melonlorrd's Blog</a>

    <header>
      <h1>{{ .Title }}</h1>
      <time>{{ .Date.Format "02 Jan 2006" }}</time>
    </header>

    <article>
      {{ .HTML }}
    </article>
  </main>
</body>
</html>
